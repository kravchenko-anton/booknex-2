/// <reference types="react" />
import type { ButtonProperties } from './types';
declare const Button: ({ size, variant, disabled, isLoading, icon: Icon, className, children, ...properties }: ButtonProperties) => import("react").JSX.Element;
export default Button;
