import { SCREEN_HEIGHT, SCREEN_WIDTH } from '@/utils/dimensions'

export const settings = {
	size: {
		full: {
			width: '100%' as '100%',
			height: '100%' as '100%'
		},
		screen: {
			width: SCREEN_WIDTH,
			height: SCREEN_HEIGHT
		}
	}
}
